# Fundamentals

# Configuration

```
// SPDX-License-Identifier: GPL-3.0 
// The above line is to indicate that the contract is open source


pragma solidity ^0.8.0;
// The above line indicates the version of solidity compiler
// "^" is used for getting the versions after the specified version
// In this example the compiler uses versions after 0.8.0
// The versioning is called semantic versioning --> major.minor.bug 



```
